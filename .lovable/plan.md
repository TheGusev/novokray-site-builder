## Что сделаю

Размещу счётчик Яндекс.Метрики (ID **110968995**) на всех страницах сайта через корневой маршрут `src/routes/__root.tsx`, чтобы он работал глобально, не дублировался и не ломал SSR.

## План

1. **Основной скрипт в `<head>`**
   - Добавлю в `head().scripts` массив `src/routes/__root.tsx` скрипт с инициализацией `ym(110968995, 'init', {...})`.
   - Сохраню все параметры инициализации: `ssr:true`, `webvisor:true`, `clickmap:true`, `ecommerce:"dataLayer"`, `referrer: document.referrer`, `url: location.href`, `accurateTrackBounce:true`, `trackLinks:true`.
   - Скрипт будет рендериться как строка и выполняться только в браузере после гидратации — `document`/`location` недоступны на сервере, но это нормально, так как скрипт не выполняется во время SSR.

2. **`<noscript>`-пиксель в `<body>`**
   - Добавлю в `RootShell` (после `<Scripts />` или перед `</body>`) fallback-изображение `https://mc.yandex.ru/watch/110968995` внутри `<noscript><div>...<img ... /></div></noscript>`.
   - Размещу именно в `<body>`, не в `<head>` — в `<head>` flow-контент (`<img>`) запрещён и может вызвать ошибки парсинга SSR.

3. **Проверка**
   - Запущу typecheck/build, чтобы убедиться, что добавление скрипта не сломало сборку.
   - Проверю исходный код страницы в preview, что `<script>` с метрикой присутствует в `<head>`, а `<noscript>` — в `<body>`.

## Что не трогаю

- Не меняю логику других скриптов (JSON-LD Schema.org остаётся).
- Не добавляю дополнительные счётчики или аналитику.
- Не меняю структуру остальных компонентов.