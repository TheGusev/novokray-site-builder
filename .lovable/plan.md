# Переобход в Яндекс.Вебмастере — список ссылок и проверка

## Что уже настроено (проверено)

- **Домен во всех источниках**: `https://dez-federation.ru` (SITE.domain, robots.txt, sitemap).
- **Sitemap**: `https://dez-federation.ru/sitemap.xml` — генерируется динамически из `SITE`, `SERVICES` (14), `CITIES` (10), `DISTRICTS` (11), `POSTS` (60), `DOCS` (6) + 13 статических + 4 хаба услуг = **118 URL**.
- **robots.txt**: `Sitemap:` прописан, `Clean-param` для UTM, закрыты `/admin`, `/api/`, `/dogovor/zapolnit`, параметрические страницы блога.
- **Подтверждение прав в Яндексе**: HTML-файл `yandex_49b530d04291abee.html` (см. предыдущий шаг) — добавить в `public/` при готовности.

## Что я сделаю

1. Сгенерирую файл **`public/yandex-recrawl.txt`** — плоский список всех 118 URL (по одному в строке), удобно копировать в Яндекс.Вебмастер → «Индексирование → Переобход страниц» (лимит 30/день на бесплатных, 210 на подтверждённых доменах — файл разбит на приоритетные секции сверху вниз).
2. Порядок секций в файле:
   - Приоритет 1: главная, `/services`, `/price`, `/garantii`, `/contacts`, `/kp`, `/blog`, `/faq`, `/o-kompanii`, `/category/dezinfekciya-novosibirsk`, `/karta-sayta`, `/privacy`, `/terms`
   - Приоритет 2: 4 хаба `/uslugi/*`
   - Приоритет 3: 14 `/services/*`
   - Приоритет 4: 10 `/gorod/*` и 11 `/raion/*`
   - Приоритет 5: 60 `/blog/*`
   - Приоритет 6: 6 `/docs/*`
3. Продублирую этот же список как маршрут **`/yandex-recrawl.txt`** (server route), чтобы всегда открывался актуально и не устаревал при добавлении статей/услуг.
4. Проверю соответствие:
   - все пути в sitemap реально существуют в `routeTree.gen.ts`;
   - ни один URL из списка не заблокирован `robots.txt` (`/dogovor/zapolnit`, `/admin`, `/api/` исключены);
   - `lastmod` у блога = `post.date`, у остальных = сегодня;
   - канонические URL и OG используют `SITE.domain` (уже так).
5. Отчёт в чате: суммарное число, ссылки для Яндекса (sitemap, robots, txt-файл), найденные несоответствия — если будут.

## Что от вас

- Подтвердить план — я всё сгенерирую и выдам ссылки одним сообщением.
- Отдельно скажите, класть ли `yandex_49b530d04291abee.html` в `public/` сейчас (для подтверждения прав).
