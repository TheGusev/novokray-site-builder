
## Что исправлю

### 1. Мобильное меню (баг с двойной шапкой)
В `Header.tsx` мобильная панель (`<aside>`) рендерит свой собственный `<Logo />` + кнопку закрытия, и при этом основная шапка `header` остаётся видимой сверху — получается «две одинаковые плашки». Перепишу на компонент `Sheet` из `@/components/ui/sheet` (right side, полноэкранный на мобиле), уберу дублирующий `<Logo />` внутри панели — оставлю только заголовок «Меню» и крестик. Также проверю, что клики по `<Link>` внутри корректно закрывают `Sheet` и переходят по роутам (использую `SheetClose asChild` или `onOpenChange`).

### 2. Нижняя плавающая CTA
Полностью удалю `StickyMobileCTA` из `__root.tsx` и сам файл. Кнопки «Позвонить» и WhatsApp останутся в шапке и в формах на страницах.

### 3. Затемнение фотографий
Сейчас почти все hero/секции используют `bg-primary` + полупрозрачное изображение или градиент `from-primary/80`. Пройдусь по:
- `routes/index.tsx` (hero, gallery, geo, b2b)
- `routes/services.$slug.tsx` (hero, photo strip)
- `routes/o-kompanii.tsx`, `routes/price.tsx`, `routes/garantii.tsx`, `routes/contacts.tsx`, `routes/category.dezinfekciya-novosibirsk.tsx`, `routes/blog.$slug.tsx`, `routes/blog.index.tsx`, `routes/services.index.tsx`, `routes/faq.tsx`

Заменю синие оверлеи на:
- либо без оверлея вовсе (фото на светлом фоне в карточке)
- либо лёгкий нижний градиент `from-black/55 via-black/10 to-transparent` только под текстом, чтобы фото оставалось чётким и цветным
Текст, который раньше лежал поверх синей заливки, перенесу в отдельный блок рядом с фото или в нижнюю «подложку» карточки.

### 4. Читаемость текста
Везде, где текст сейчас белый на синем — переведу на тёмный текст на светлой подложке (`bg-card`, `bg-surface`) или на ярко-белый только в зонах с тёмным градиентом снизу. Проверю контраст на мобильном.

### 5. Анимация «волны» по тексту (цветная)
Добавлю новый компонент `WaveText.tsx`:
- разбивает текст на символы (`<span>` на каждую букву)
- бесконечная keyframe-анимация: цвет `color` плавно бежит по буквам через `animation-delay: calc(var(--i) * 60ms)`
- градиент `primary → accent → primary-glow` через `background-clip: text` либо смена `color` через keyframes
- параметр `once` для одноразового запуска, по умолчанию loop
Добавлю keyframes `wave-color` и утилиту в `src/styles.css`. Применю к ключевым заголовкам H1/H2 на всех страницах (заменю там, где сейчас стоит `AnimatedHeading`, на `WaveText` для основных заголовков секций; обычные `AnimatedHeading` оставлю там, где волна была бы избыточна).

### Технические детали
- Файлы для правки: `src/components/site/Header.tsx`, `src/routes/__root.tsx`, удаление `src/components/site/StickyMobileCTA.tsx`, новый `src/components/site/WaveText.tsx`, `src/styles.css`, плюс все route-файлы из п.3-5.
- Не трогаю: данные (`services.ts`, `blog.ts`), генерацию новых картинок, бэкенд.

### Чего НЕ делаю
- Не включаю Lovable Cloud (вы не просили).
- Не перегенерирую фото — только убираю синие оверлеи.
- Не меняю структуру роутов.

После внедрения проверю на мобильном viewport (393×617): меню открывается одной панелью без второй шапки, ссылки переходят, нижней CTA нет, фото чёткие, заголовки «бегут» волной.
